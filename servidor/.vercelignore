# Sem este arquivo o Vercel CLI usa o .gitignore — e nucleo/ esta la, porque
# nao pode virar copia commitada. Resultado: a copia de build ficaria de fora
# do pacote. Aqui a lista e so o que realmente nao sobe.
node_modules
.vercel
.env
.env.local
.env.*.local
.agents
.claude
skills-lock.json
